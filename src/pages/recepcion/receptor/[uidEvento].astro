---
import LayoutLector from '../../../layouts/LayoutLector.astro'
import MainDashboard from '../../../components/MainDasboard.astro'
import CardCheckIn from './componentes/CardCheckIn.astro';
const {uidEvento}=Astro.params

---


<LayoutLector title="RecepcionQR">

    <MainDashboard>
<h1>Nombre De Evento</h1>

<div class="p-10 flex mx-auto w-full items-center justify-center rounded shadow-sm bg-white min-h-[75vh]">
 
    <ul class="flex flex-col bg-paleta1-secondary rounded-md  p-4 w-full">
        <CardCheckIn/>
       
    </ul>
</div>
</MainDashboard>
</LayoutLector>


<script is:inline define:vars={{uidEvento}}>
    // Crear un nuevo objeto EventSource
    const source = new EventSource('http://localhost:8000/event-stream?uidEvento='+uidEvento);
  
    // Escuchar el evento 'message'
    source.onmessage = (event) => {
      console.log(event);
    };
  </script>