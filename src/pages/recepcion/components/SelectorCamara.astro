---

const {name, id,clase}=Astro.props
---
<select
      name={name}
      id={id}
      class={`${clase} text-xs text-gray-700 rounded p-1 w-full border-transparent focus:ring-o bg-gray-200 `}
    >
      <option
        value="selectCamara"
        selected
        disabled
        class="text-xs text-gray-700">Selecciona una opcion</option
      >
    </select>